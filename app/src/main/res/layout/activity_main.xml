<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_centerVertical="true"
    android:gravity="center|center_vertical"
    android:textAlignment="gravity"
    tools:context=".MainActivity">

    <!-- First Part: Logo -->
    <ImageView
        android:id="@+id/logo"
        android:layout_width="128dp"
        android:layout_height="128dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:padding="16dp"
        android:src="@drawable/smojify_logo_200" />

    <!-- Second Part: Launch Button -->
    <Button
        android:id="@+id/button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/logo"
        android:layout_centerHorizontal="true"
        android:padding="16dp"
        android:text="Launch Smojify" />

    <!-- Third Part: Display Reacting Emoji -->
    <TextView
        android:id="@+id/reacting_emoji"
        android:layout_width="128dp"
        android:layout_height="128dp"
        android:layout_below="@id/button"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:textColor="@android:color/white"
        android:textSize="48sp"
        android:gravity="center_vertical|center_horizontal" />

    <!-- Fourth Part: Text Input -->
    <EditText
        android:id="@+id/emoji_input"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/reacting_emoji"
        android:hint="Type Emojis Here"
        android:inputType="text"
        android:padding="16dp"
        android:textCursorDrawable="@null" />

    <!-- Fifth Part: Display Current Song Info -->
    <TextView
        android:id="@+id/current_song_info"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/emoji_input"
        android:layout_marginTop="16dp"
        android:padding="16dp"
        android:text="Current Song Info"
        android:textAlignment="center"
        android:textSize="24sp" />

</RelativeLayout>
